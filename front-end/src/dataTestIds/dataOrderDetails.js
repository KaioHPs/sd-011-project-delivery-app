const prefix = 'seller_order_details__';

const orderId = `${prefix}element-order-details-label-order-id-`;
const orderDate = `${prefix}element-order-details-label-order-date-`;
const deliveryStatus = `${prefix}element-order-details-label-delivery-status-`;
const preparingCheck = `${prefix}button-preparing-check-`;
const dispatchCheck = `${prefix}button-dispatch-check-`;
const totalPrice = `${prefix}element-order-total-price-`;

module.exports = {
  orderId,
  orderDate,
  deliveryStatus,
  preparingCheck,
  dispatchCheck,
  totalPrice,
};
